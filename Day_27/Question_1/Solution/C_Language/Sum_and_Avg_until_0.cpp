#include<iostream>
using namespace std;
int main()
{
   double x,sum = 0,avg = 0,i;
    while (x != 0){
  cout<<"Enter the number : ";
  cin>>x;

    sum = sum + x;

    avg = sum / i;
  i++;
}
cout<<"Sum of the numbers is "<<sum<<endl;
cout<<"Average of the numbers is "<<avg;
return 0;
}
